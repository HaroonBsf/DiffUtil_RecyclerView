<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DiffUtilsRecyclerView" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="android:statusBarColor">@color/app_theme</item>
        <item name="android:navigationBarColor">@color/app_theme</item>
    </style>

    <style name="CustomTabText" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/item_color</item>
    </style>

    <style name="ShapeAppearanceOverlay" parent="">
        <item name="cornerSize">10%</item>
    </style>

</resources>